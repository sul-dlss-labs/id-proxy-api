version: '3.2'

services:
  localstack:
    image: localstack/localstack
    ports:
      - "4567-4582:4567-4582"
      - "8080:8080"
    environment:
      - SERVICES=lambda
      - DEFAULT_REGION=us-east-1
